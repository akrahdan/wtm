import { NewPasswordComponent } from './new-password.component'
import { Routes } from '@angular/router';

export const newPasswordRoutes: Routes = [
  { path: 'users/password/new', component: NewPasswordComponent }
];
  